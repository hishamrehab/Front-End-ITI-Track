import { HiglightCard } from './higlight-card';

describe('HiglightCard', () => {
  it('should create an instance', () => {
    const directive = new HiglightCard();
    expect(directive).toBeTruthy();
  });
});
